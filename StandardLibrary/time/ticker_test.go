package __

import (
	"testing"
	"time"
)

func TestNewTicker(t *testing.T) {
	for {
		select {
		case
		}
	}
	tt := time.NewTicker(1)
	if cap
}

func TestNewTimer(t *testing.T) {
	select {
	case <-time.NewTimer(time.Second).C:
		return
	case v := <-inChan:
	case outChan <- v:
	}
}
